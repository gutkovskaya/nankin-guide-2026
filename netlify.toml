# Статический сайт-путеводитель. Сборки нет — Netlify публикует корень репозитория.
[build]
  publish = "."

# Небольшое кеш-правило для картинок (можно убрать).
[[headers]]
  for = "/img/*"
  [headers.values]
    Cache-Control = "public, max-age=86400"
